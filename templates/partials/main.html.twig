<section class="content">
  <article class="markdown-section" id="main">
    
    {% if page.header.show_title == true %}
      <h1>{{ page.title }}</h1>
    {% endif %}

    {{ page.content }}
    
  </article>
</section>